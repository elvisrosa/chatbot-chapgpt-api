import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '09-chatbot-gpt',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
